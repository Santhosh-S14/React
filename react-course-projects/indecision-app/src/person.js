const isAdult = (x) => {
    return x >= 18;
};

const canDrink = (y) => {
    return y >= 21;
};

const isSenior = (z) => {
    return z >= 65;
};

export {isAdult, canDrink};
export default isSenior;